window.setTimeout(tester, 100);

function tester() {
	/*p.paymentObj[0]["Payment"] = 1000000;
	console.log(p.paymentObj[0]["Payment"]);

	p.paymentObj = p.recalPObj();
	console.log(p.paymentObj[0]["Payment"]);

	makeTable(p.paymentObj);*/
}
